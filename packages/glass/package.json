{
  "name": "@cuatro/glass",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GLASS — the CUATRO padel rating engine. Pure, deterministic, exhaustively-tested ELO-derived rating with Placement, Echo Damping, Confidence, and an explainable Ledger.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
